<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.MyMapper">
    <select id="getStudentInfo" resultType="com.example.demo.dao.Student">
        select * from student where id=#{id}
    </select>

    <insert id="addStudent" parameterType="com.example.demo.dao.Student">
        insert into student(id,age,name) values(#{id},#{age},#{name})
    </insert>

    <delete id="deleteStudentById" parameterType="int">
        delete from student where id=#{id}
    </delete>

    <update id="modifyStudent" parameterType="com.example.demo.dao.Student">
        update student
        <set>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="name != null">
                name = #{name}
            </if>
        </set>
        where id=#{id}
    </update>
</mapper>
